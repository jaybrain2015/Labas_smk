apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: labas-smk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
        - name: ai-service
          image: jaybrain/labas-fastapi:b470816232a132fa31101335798b2f959e7ed94d
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: labas-secrets
                  key: ANTHROPIC_API_KEY
          ports:
            - containerPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: labas-smk
spec:
  selector:
    app: ai-service
  ports:
    - port: 8001
      targetPort: 8001
