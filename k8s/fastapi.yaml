apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: labas-smk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
        - name: ai-service
          image: jaybrain/labas-fastapi:c33fdc6d725b13f78f7f06c89fa4750a4b810c91
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: labas-secrets
                  key: ANTHROPIC_API_KEY
          ports:
            - containerPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: labas-smk
spec:
  selector:
    app: ai-service
  ports:
    - port: 8001
      targetPort: 8001
